name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend Image
        run: |
          docker build -t vivekjaiswal2003/mean-backend:latest ./backend
          docker push vivekjaiswal2003/mean-backend:latest

      - name: Build Frontend Image
        run: |
          docker build -t vivekjaiswal2003/mean-frontend:latest ./frontend
          docker push vivekjaiswal2003/mean-frontend:latest